# podcast-to-course ingest 层依赖（Python 3.10+）
# 仅 ingest（音频下载/解析 + 可选的通义听悟 provider）需要；课程生成逻辑本身不依赖这些包。

requests>=2.31.0           # 所有 HTTP 请求（小宇宙页面、结果下载）
beautifulsoup4>=4.12.0     # 解析小宇宙页面 HTML（og:audio / JSON-LD）
aliyun-python-sdk-core>=2.15.0   # 通义听悟 API 的 ROA 签名（CommonRequest）
